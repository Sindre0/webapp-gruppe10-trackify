const APP_VERSION = "v1"
const API_URL = `/api/${APP_VERSION}`;
const API_ENDPOINTS = {
  LEADERBOARDS: `${API_URL}/leaderboards`,
  USERS: `${API_URL}/users`,
};

export { APP_VERSION, API_URL, API_ENDPOINTS };